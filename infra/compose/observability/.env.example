GRAFANA_USER=admin
GRAFANA_PASSWORD=change-me

# Credenciais do Postgres do orchestrator (.11) p/ o datasource do Grafana (MAC-35).
# Devem bater EXATAMENTE com as do .env do orchestrator (POSTGRES_USER/PASSWORD/DB).
POSTGRES_USER=orchestrator
POSTGRES_PASSWORD=change-me
POSTGRES_DB=orchestrator
