---
phase: 03-plane-only-provider-cutover
plan: "03"
type: execute
wave: 3
depends_on: ["03-02"]
files_modified:
  - apps/orchestrator-api/src/queue.ts
  - apps/orchestrator-api/src/queue.test.ts
  - apps/orchestrator-api/src/worker.ts
  - apps/orchestrator-api/src/worker.test.ts
  - apps/orchestrator-api/src/scheduleWorker.ts
  - apps/orchestrator-api/src/scheduleWorker.test.ts
autonomous: false
requirements: [PLN-02, PLN-03, PLN-04]
user_setup:
  - service: deployed Redis/BullMQ queue
    why: "Live Redis/BullMQ state was not inspected; legacy jobs may lack cardProvider and must be inspected or drained before deploy fallback removal."
    dashboard_config:
      - task: "Inspect or drain old `agent-runs` BullMQ jobs with missing cardProvider before deploying code that rejects ambiguous payloads."
        location: "orchestrator Redis container on LXC 201 or the deployed BullMQ inspection surface"
must_haves:
  truths:
    - "D-01: Queue, worker, and scheduler paths do not silently route ambiguous new work to Linear."
    - "D-02: Old BullMQ jobs with missing provider data can still resolve safely through persisted run provider/card fields when those fields exist."
    - "D-03: Queue/worker/scheduler behavior changes are driven by failing tests before implementation."
    - "D-05: Legacy Linear compatibility is retained only for explicit legacy rows or explicitly inspected old queue payloads."
  artifacts:
    - "apps/orchestrator-api/src/queue.test.ts"
    - "apps/orchestrator-api/src/worker.test.ts"
    - "apps/orchestrator-api/src/scheduleWorker.test.ts"
    - "Blocking operator checkpoint for legacy BullMQ queue inspection/drain."
  key_links:
    - "Worker loads persisted run identity before deciding whether a legacy queued payload is safe."
    - "Queue helper rejects ambiguous missing-provider payloads only after persisted-run compatibility is available."
    - "Scheduler-created cards use Plane scheduled label config and never substitute legacy scheduled labels for new work."
---

<objective>
Cut queue, worker, and scheduler provider resolution over to Plane-only active behavior while preserving safe handling for old BullMQ jobs.

Purpose: Remove implicit Linear defaults from asynchronous runtime paths without breaking queued jobs that can be resolved from persisted run data per D-01, D-02, D-03, and D-05.
Output: Queue/worker/scheduler tests and code, plus a blocking operator checkpoint for live Redis/BullMQ payload inspection before deploy fallback removal.
</objective>

<execution_context>
@/root/.codex/gsd-core/workflows/execute-plan.md
@/root/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-plane-only-provider-cutover/03-CONTEXT.md
@.planning/phases/03-plane-only-provider-cutover/03-RESEARCH.md
@.planning/phases/03-plane-only-provider-cutover/03-VALIDATION.md
@.planning/phases/03-plane-only-provider-cutover/03-PATTERNS.md
@.planning/phases/03-plane-only-provider-cutover/03-02-SUMMARY.md
@CLAUDE.md
@AGENTS.md
</context>

## Source Coverage Audit

| SOURCE | ID | Feature/Requirement | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | - | Remove Linear as an active provider while preserving old-data compatibility. | 03-03 | COVERED | Async runtime paths are cut over without dropping compatibility. |
| REQ | PLN-02 | Plane is the only active card-provider path unless compatibility is proven necessary. | 03-03 | COVERED | Queue/worker/scheduler no longer infer Linear for ambiguous new work. |
| REQ | PLN-03 | Scheduler and async runtime behavior are covered by Plane-focused tests. | 03-03 | COVERED | Queue, worker, and scheduler tests are added or updated. |
| REQ | PLN-04 | Legacy data/schema handling has explicit compatibility rule. | 03-03 | COVERED | Old jobs can resolve from persisted run provider/card fields. |
| RESEARCH | R-02 | Queue and worker need explicit provider coverage and old BullMQ job handling. | 03-03 | COVERED | This plan owns code compatibility and operator queue checkpoint. |
| RESEARCH | R-03 | Scheduler-created cards need Plane label coverage. | 03-03 | COVERED | Scheduler tests assert Plane label behavior. |
| RESEARCH | R-06 | Live Redis/BullMQ state was not inspected. | 03-03 | COVERED | Blocking checkpoint requires operator inspection/drain. |
| CONTEXT | D-01 | Plane-only active provider for new work. | 03-03 | COVERED | Task actions cite D-01. |
| CONTEXT | D-02 | Existing legacy rows remain readable. | 03-03 | COVERED | Worker resolves old jobs from persisted runs before rejecting ambiguity. |
| CONTEXT | D-03 | Test-first cutover. | 03-03 | COVERED | Tasks use RED-GREEN before production edits. |
| CONTEXT | D-05 | Avoid destructive deletion. | 03-03 | COVERED | No package, route, schema, or doc deletion occurs. |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED-GREEN queue and worker provider resolution with legacy job compatibility</name>
  <files>apps/orchestrator-api/src/queue.ts, apps/orchestrator-api/src/queue.test.ts, apps/orchestrator-api/src/worker.ts, apps/orchestrator-api/src/worker.test.ts</files>
  <read_first>
    .planning/phases/03-plane-only-provider-cutover/03-CONTEXT.md
    .planning/phases/03-plane-only-provider-cutover/03-RESEARCH.md
    .planning/phases/03-plane-only-provider-cutover/03-PATTERNS.md
    .planning/phases/03-plane-only-provider-cutover/03-02-SUMMARY.md
    apps/orchestrator-api/src/queue.ts
    apps/orchestrator-api/src/queue.test.ts
    apps/orchestrator-api/src/worker.ts
    apps/orchestrator-api/src/workerManager.test.ts
    apps/orchestrator-api/src/runs.ts
  </read_first>
  <behavior>
    - RED: New plan jobs that lack both `cardProvider` and a persisted run provider/card fail with a clear provider/card error.
    - RED: Old BullMQ plan jobs that lack `cardProvider` but have `runId` still process when `getRun(runId)` has valid `cardProvider` and `cardId`.
    - RED: Explicit legacy plan jobs still resolve when `cardProvider: 'linear'` is present.
    - RED: Worker resume/comment/continuation paths use persisted `run.cardProvider` and `run.cardId`; missing provider data fails clearly instead of inferring Linear.
  </behavior>
  <action>
    RED: Add failing target tests in `queue.test.ts` and `worker.test.ts` for the behavior above. Follow the mock style in `workerManager.test.ts`. Run the focused command and record the expected failures before production edits.

    GREEN: Update `queue.ts` so provider/card validation supports two safe paths per D-01 and D-02: explicit job provider/card data, or persisted run provider/card data supplied by the worker. Keep explicit `cardProvider: 'linear'` compatibility for legacy jobs that say they are Linear. Reject ambiguous jobs that lack provider data and cannot be resolved from a persisted run; do not default missing provider to Linear.

    GREEN: Update `worker.ts` so plan, resume, cost alert, auto-approval comment, and research-to-landing continuation paths load and use persisted generic run fields before looking at job payload compatibility. Permit a legacy Linear path only when the persisted row or job explicitly identifies `cardProvider: 'linear'`; otherwise throw a clear provider/card error per D-01.

    REFACTOR: Keep private helper names stable where possible. If a helper is introduced for provider validation, keep it local to the owning module unless tests already need a public function.
  </action>
  <acceptance_criteria>
    - RED evidence is recorded in the task commit or summary: queue/worker target tests fail before implementation.
    - Behavior assertion: missing provider in a new plan job throws a clear error when persisted run data cannot resolve it.
    - Behavior assertion: old queued plan job data without provider resolves through persisted run `cardProvider` and `cardId`.
    - Behavior assertion: explicit legacy Linear plan job data still resolves for compatibility.
    - Behavior assertion: worker resume/comment/continuation uses persisted provider/card fields and does not infer Linear for ambiguous rows.
  </acceptance_criteria>
  <verify>
    <automated>rtk corepack pnpm test -- apps/orchestrator-api/src/queue.test.ts apps/orchestrator-api/src/worker.test.ts</automated>
    <automated>rtk bash -lc "! rg -n \"\\?\\? 'linear'|\\?\\? \\\"linear\\\"\" apps/orchestrator-api/src/queue.ts apps/orchestrator-api/src/worker.ts"</automated>
  </verify>
  <done>Queue and worker provider resolution use explicit or persisted provider/card data and keep old queued jobs safe when persisted run data exists.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: RED-GREEN scheduler-created Plane card labels</name>
  <files>apps/orchestrator-api/src/scheduleWorker.ts, apps/orchestrator-api/src/scheduleWorker.test.ts</files>
  <read_first>
    .planning/phases/03-plane-only-provider-cutover/03-CONTEXT.md
    .planning/phases/03-plane-only-provider-cutover/03-RESEARCH.md
    .planning/phases/03-plane-only-provider-cutover/03-PATTERNS.md
    .planning/phases/03-plane-only-provider-cutover/03-02-SUMMARY.md
    apps/orchestrator-api/src/scheduleWorker.ts
    apps/orchestrator-api/src/scheduleWorker.test.ts
    apps/orchestrator-api/src/routes/schedules.test.ts
  </read_first>
  <behavior>
    - RED: Scheduler-created cards use `PLANE_SCHEDULED_LABEL_ID` when configured.
    - RED: Scheduler-created cards pass no scheduled label when the Plane scheduled label is absent.
    - RED: Scheduler-created cards never substitute a legacy scheduled label for new Plane work.
  </behavior>
  <action>
    RED: Add failing target tests in `scheduleWorker.test.ts` for Plane scheduled label behavior. Follow the route-test/mock style from `routes/schedules.test.ts`. Run the focused command and record the expected failures before production edits.

    GREEN: Update `scheduleWorker.ts` so scheduler-created cards use `PLANE_SCHEDULED_LABEL_ID` when configured and pass no scheduled label when it is absent. Do not use the legacy scheduled label for new scheduled cards per D-01.

    REFACTOR: Keep scheduler queue and schedule persistence behavior unchanged; this task changes only provider label selection for created cards.
  </action>
  <acceptance_criteria>
    - RED evidence is recorded in the task commit or summary: scheduler target tests fail before implementation.
    - Behavior assertion: configured Plane scheduled label is used for scheduled cards.
    - Behavior assertion: absent Plane scheduled label means no scheduled label is passed.
    - Source assertion: scheduler-created cards do not use legacy scheduled label env for new work.
  </acceptance_criteria>
  <verify>
    <automated>rtk corepack pnpm test -- apps/orchestrator-api/src/scheduleWorker.test.ts</automated>
    <automated>rtk bash -lc "! rg -n 'LINEAR_SCHEDULED_LABEL_ID' apps/orchestrator-api/src/scheduleWorker.ts"</automated>
  </verify>
  <done>Scheduler-created cards use Plane scheduled labels only and keep new scheduled work out of legacy Linear paths.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Blocking operator legacy BullMQ queue checkpoint</name>
  <files>N/A - operator checkpoint only</files>
  <action>Block execution after the code compatibility path is implemented until the operator inspects or drains live legacy BullMQ payloads. This checkpoint exists because research did not inspect live Redis/BullMQ state, so the plan must not claim old queued jobs are safe in deployment without operator evidence.</action>
  <what-built>Queue and worker compatibility now process old missing-provider BullMQ payloads only when persisted run provider/card data exists, and reject unresolved ambiguous payloads.</what-built>
  <how-to-verify>
    1. Before deploying the fallback removal, inspect the deployed `agent-runs` BullMQ queue on LXC 201 or the active orchestrator host for waiting, delayed, failed, and paused jobs whose `data.kind` is `plan` and whose payload lacks `cardProvider`.
    2. For each old payload, confirm the referenced `runId` has persisted `card_provider` and `card_id` in Postgres, then either let it finish under the compatibility path or drain/remove it intentionally.
    3. Confirm no unresolved waiting/delayed plan jobs remain with missing `cardProvider` and no persisted run provider/card data.
  </how-to-verify>
  <verify>
    <automated>rtk bash -lc "echo 'Operator checkpoint: inspect/drain legacy BullMQ payloads before deploy fallback removal'"</automated>
  </verify>
  <done>Operator confirms no unresolved waiting/delayed plan jobs remain with missing `cardProvider` and no persisted run provider/card data.</done>
  <resume-signal>Type "approved" after queue inspection/drain is complete, or describe the remaining legacy payloads to replan compatibility.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| BullMQ job payload -> worker | Queue payloads may be stale, malformed, or missing provider fields. |
| Persisted runs -> worker/report comments | Old rows influence provider selection during resume, comments, and continuation. |
| Scheduler -> cards gateway | Internal cron-generated work creates cards and runs without human webhook input. |
| Operator -> deployed Redis | Manual queue inspection changes live deployment readiness and must be explicit. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-03-03-01 | Tampering | `queue.ts` / `worker.ts` | high | mitigate | Require explicit or persisted provider/card data and throw on unresolved ambiguity instead of inferring legacy provider. |
| T-03-03-02 | Denial of Service | old BullMQ jobs | high | mitigate | Preserve persisted-run compatibility path and require blocking operator queue inspection/drain before deploy fallback removal. |
| T-03-03-03 | Tampering | `scheduleWorker.ts` | medium | mitigate | Use Plane scheduled label config only and remove legacy scheduled label substitution for new cards. |
| T-03-03-SC | Tampering | package installs | high | mitigate | No package installs are planned; package/dependency deletion is explicitly forbidden by D-05. |
</threat_model>

<verification>
Run focused checks after tasks:

<automated>rtk corepack pnpm test -- apps/orchestrator-api/src/queue.test.ts apps/orchestrator-api/src/worker.test.ts apps/orchestrator-api/src/scheduleWorker.test.ts</automated>
<automated>rtk bash -lc "! rg -n \"\\?\\? 'linear'|\\?\\? \\\"linear\\\"|LINEAR_SCHEDULED_LABEL_ID\" apps/orchestrator-api/src/queue.ts apps/orchestrator-api/src/worker.ts apps/orchestrator-api/src/scheduleWorker.ts"</automated>
</verification>

<success_criteria>
- Queue and worker tests prove old missing-provider jobs are compatible only through persisted run data.
- Queue and worker tests prove unresolved ambiguous provider state does not default to Linear.
- Scheduler tests prove new scheduled cards use Plane label config only.
- The operator has inspected or drained live legacy BullMQ payloads before deploying fallback removal.
- No package, route, schema column, dashboard, or documentation deletion occurs in this plan.
</success_criteria>

<output>
Create `.planning/phases/03-plane-only-provider-cutover/03-03-SUMMARY.md` when done.
</output>
