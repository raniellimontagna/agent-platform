---
phase: 03-plane-only-provider-cutover
plan: "04"
type: execute
wave: 4
depends_on: ["03-03"]
files_modified:
  - apps/orchestrator-api/src/routes/webhooks.ts
  - apps/orchestrator-api/src/routes/webhooks.test.ts
  - apps/orchestrator-api/.env.example
  - infra/compose/orchestrator/.env.example
autonomous: false
requirements: [PLN-01, PLN-02, PLN-03]
user_setup:
  - service: deployed orchestrator environment and webhook exposure
    why: "Live deployment secrets, Tailscale Funnel, and provider webhook UI state were not inspected by research."
    env_vars:
      - name: CARD_EXTRA_PROVIDERS
        source: "orchestrator runtime .env on LXC 201 or deployment secret store"
    dashboard_config:
      - task: "Confirm deployed env does not set `CARD_EXTRA_PROVIDERS=linear` unless legacy compatibility is intentionally enabled."
        location: "orchestrator runtime env on LXC 201"
      - task: "Confirm any live Linear webhook registration is legacy compatibility only and not active default intake."
        location: "Tailscale Funnel status plus Linear provider webhook UI"
must_haves:
  truths:
    - "D-01: Plane webhook remains the active intake path and env examples no longer enable Linear by default."
    - "D-03: Webhook gating behavior is driven by failing tests before implementation."
    - "D-04: Active env/operator examples describe Plane-only operation with Linear as legacy/migration-only compatibility."
    - "D-05: `/webhooks/linear` route support is not deleted without destructive confirmation."
  artifacts:
    - "Updated Plane and legacy webhook route tests."
    - "Updated webhook gating in apps/orchestrator-api/src/routes/webhooks.ts."
    - "Updated Plane-only env examples."
    - "Blocking operator checkpoint for deployed env and webhook exposure."
  key_links:
    - "Legacy webhook route is disabled unless explicit legacy provider config is present."
    - "Plane webhook signature validation and active work-item behavior remain unchanged."
    - "Env examples match the Plane-only runtime defaults from Plan 03-02."
---

<objective>
Gate legacy webhook behavior behind explicit compatibility config and make env examples Plane-only.

Purpose: Move external intake and deployment examples to Plane-only active operation without deleting route support or claiming live webhook/env state was inspected per D-01, D-03, D-04, and D-05.
Output: Webhook route tests/code, Plane-only env examples, and an operator checkpoint for deployed env and webhook exposure.
</objective>

<execution_context>
@/root/.codex/gsd-core/workflows/execute-plan.md
@/root/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-plane-only-provider-cutover/03-CONTEXT.md
@.planning/phases/03-plane-only-provider-cutover/03-RESEARCH.md
@.planning/phases/03-plane-only-provider-cutover/03-VALIDATION.md
@.planning/phases/03-plane-only-provider-cutover/03-PATTERNS.md
@.planning/phases/03-plane-only-provider-cutover/03-03-SUMMARY.md
@docs/runbooks/webhook-tailscale.md
@CLAUDE.md
@AGENTS.md
</context>

## Source Coverage Audit

| SOURCE | ID | Feature/Requirement | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | - | Remove Linear as an active provider while preserving compatibility. | 03-04 | COVERED | Webhook and env defaults are cut over without route deletion. |
| REQ | PLN-01 | Linear env/webhook inventory remains actionable. | 03-04 | COVERED | Env examples and static route checks keep compatibility visible. |
| REQ | PLN-02 | Plane is the only active provider path. | 03-04 | COVERED | Linear webhook is disabled unless explicit legacy config exists. |
| REQ | PLN-03 | Webhook intake and approval behavior are covered by Plane-focused tests. | 03-04 | COVERED | Plane webhook tests remain active while legacy route is gated. |
| RESEARCH | R-01 | Stop default config from enabling legacy provider. | 03-04 | COVERED | Env examples remove legacy extra provider from defaults. |
| RESEARCH | R-06 | Live webhook/env state was not inspected. | 03-04 | COVERED | Blocking operator checkpoint covers deployed env and webhook exposure. |
| CONTEXT | D-01 | Plane-only active provider for new intake. | 03-04 | COVERED | Task action cites D-01. |
| CONTEXT | D-03 | Test-first cutover. | 03-04 | COVERED | Task uses RED-GREEN before production edits. |
| CONTEXT | D-04 | Current ops docs/examples state Plane-only active operation. | 03-04 | COVERED | Env examples are updated here; docs follow in 03-05. |
| CONTEXT | D-05 | Do not delete route support without confirmation. | 03-04 | COVERED | `/webhooks/linear` remains present and gated. |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED-GREEN legacy webhook gating and Plane-only env examples</name>
  <files>apps/orchestrator-api/src/routes/webhooks.ts, apps/orchestrator-api/src/routes/webhooks.test.ts, apps/orchestrator-api/.env.example, infra/compose/orchestrator/.env.example</files>
  <read_first>
    .planning/phases/03-plane-only-provider-cutover/03-CONTEXT.md
    .planning/phases/03-plane-only-provider-cutover/03-RESEARCH.md
    .planning/phases/03-plane-only-provider-cutover/03-PATTERNS.md
    .planning/phases/03-plane-only-provider-cutover/03-03-SUMMARY.md
    apps/orchestrator-api/src/routes/webhooks.ts
    apps/orchestrator-api/src/routes/webhooks.test.ts
    apps/orchestrator-api/src/env.ts
    apps/orchestrator-api/.env.example
    infra/compose/orchestrator/.env.example
    docs/runbooks/webhook-tailscale.md
  </read_first>
  <behavior>
    - RED: `/webhooks/plane` continues to accept signed Plane work-item events and enqueue Plane jobs.
    - RED: `/webhooks/linear` returns a disabled legacy response unless legacy provider compatibility is explicitly configured.
    - RED: When explicit legacy compatibility is configured, `/webhooks/linear` keeps signature validation and legacy run enqueue behavior.
    - RED: Env examples show Plane-only active defaults and keep legacy keys under migration/compatibility comments.
  </behavior>
  <action>
    RED: Add failing target tests in `routes/webhooks.test.ts` for disabled-by-default legacy webhook behavior and explicit legacy compatibility behavior. Keep existing Plane webhook tests green after implementation.

    GREEN: Gate `/webhooks/linear` behind explicit legacy provider configuration derived from env. Return a clear disabled legacy response when not configured. Do not delete the route, remove Linear payload parsing, or remove Linear signature validation per D-05.

    GREEN: Update `.env.example` files so active defaults are Plane-only and legacy provider keys are documented as migration/compatibility values, not active defaults, per D-01 and D-04.

    REFACTOR: Keep Plane and Linear route behavior in the existing route hub. Broad webhook module extraction belongs to Phase 5, not this phase.
  </action>
  <acceptance_criteria>
    - RED evidence is recorded in the task commit or summary: legacy webhook target tests fail before implementation.
    - Behavior assertion: Plane webhook intake and approval tests still pass.
    - Behavior assertion: legacy webhook route is disabled by default and remains available for explicit legacy compatibility.
    - Source assertion: `/webhooks/linear` route support is not deleted.
    - Source assertion: env example active defaults do not enable the legacy provider.
  </acceptance_criteria>
  <verify>
    <automated>rtk corepack pnpm test -- apps/orchestrator-api/src/routes/webhooks.test.ts</automated>
    <automated>rtk bash -lc "rg -n \"webhooks.post\\('/webhooks/linear'\" apps/orchestrator-api/src/routes/webhooks.ts"</automated>
    <automated>rtk bash -lc "! rg -n 'CARD_EXTRA_PROVIDERS=linear' apps/orchestrator-api/.env.example infra/compose/orchestrator/.env.example"</automated>
  </verify>
  <done>Legacy webhook support is explicit compatibility only, and active env examples are Plane-only.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Blocking operator deployed env and webhook exposure checkpoint</name>
  <files>N/A - operator checkpoint only</files>
  <action>Block execution after webhook gating and env example updates until the operator inspects deployed env, Tailscale Funnel, and provider webhook UI state. This checkpoint exists because research did not inspect live deployment state, so the plan must not claim live Linear webhook exposure is disabled without operator evidence.</action>
  <what-built>The repository now treats `/webhooks/plane` as active intake and `/webhooks/linear` as disabled-by-default legacy compatibility, with Plane-only env examples.</what-built>
  <how-to-verify>
    1. Inspect the deployed orchestrator environment on LXC 201 or the active secret store and confirm `CARD_EXTRA_PROVIDERS` is empty or absent unless legacy compatibility is intentionally enabled.
    2. Inspect Tailscale Funnel status and the Linear provider webhook UI. Confirm any `/webhooks/linear` exposure is intentionally retained as legacy compatibility, or disable external Linear webhook registration without deleting the route from code.
    3. Confirm the Plane webhook registration remains active for `/webhooks/plane`.
  </how-to-verify>
  <verify>
    <automated>rtk bash -lc "echo 'Operator checkpoint: confirm deployed env and webhook exposure before deploy cutover completion'"</automated>
  </verify>
  <done>Operator confirms deployed env and webhook exposure do not leave Linear active unintentionally.</done>
  <resume-signal>Type "approved" after deployed env and webhook exposure are confirmed, or describe the live-state difference to replan deployment notes.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| External webhook -> route handler | Provider webhooks cross from public Funnel/provider UI into run creation. |
| Deployment env -> route gating | Live env controls whether legacy route compatibility is active. |
| Operator -> provider UI/Tailscale | Manual webhook exposure determines whether legacy endpoints receive traffic. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-03-04-01 | Spoofing | `/webhooks/plane` | high | mitigate | Preserve Plane signature tests and reject invalid signatures before payload semantics are trusted. |
| T-03-04-02 | Spoofing | `/webhooks/linear` | high | mitigate | Gate legacy route by explicit compatibility config and preserve HMAC validation when enabled. |
| T-03-04-03 | Information Disclosure | env examples | medium | mitigate | Keep legacy secrets documented only as compatibility values and keep sample values, not real secrets. |
| T-03-04-SC | Tampering | package installs | high | mitigate | No package installs are planned; package/dependency deletion is explicitly forbidden by D-05. |
</threat_model>

<verification>
Run focused checks after tasks:

<automated>rtk corepack pnpm test -- apps/orchestrator-api/src/routes/webhooks.test.ts</automated>
<automated>rtk bash -lc "rg -n \"webhooks.post\\('/webhooks/linear'\" apps/orchestrator-api/src/routes/webhooks.ts"</automated>
<automated>rtk bash -lc "! rg -n 'CARD_EXTRA_PROVIDERS=linear' apps/orchestrator-api/.env.example infra/compose/orchestrator/.env.example"</automated>
</verification>

<success_criteria>
- Plane webhook tests still pass for active intake, approval, auto-merge labels, and removal/cancellation behavior.
- Legacy Linear webhook route is disabled by default and remains available only for explicit compatibility.
- Env examples do not enable the legacy provider by default.
- Operator confirms live deployed env/webhook exposure does not leave Linear active unintentionally.
- `/webhooks/linear` route support is not deleted.
</success_criteria>

<output>
Create `.planning/phases/03-plane-only-provider-cutover/03-04-SUMMARY.md` when done.
</output>
