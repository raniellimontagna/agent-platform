---
phase: 03-plane-only-provider-cutover
plan: "02"
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - apps/orchestrator-api/src/cards.ts
  - apps/orchestrator-api/src/cards.test.ts
  - apps/orchestrator-api/src/env.ts
  - apps/orchestrator-api/src/env.test.ts
  - apps/orchestrator-api/src/agent.ts
  - apps/orchestrator-api/src/agent.test.ts
autonomous: true
requirements: [PLN-01, PLN-02, PLN-03]
must_haves:
  truths:
    - "D-01: Provider registry, env validation, and graph enablement are Plane-only by default for new work."
    - "D-03: Provider/env/agent behavior changes are driven by failing tests before implementation."
    - "D-05: Linear package and dependency support remain installed for explicit legacy compatibility."
  artifacts:
    - "Updated provider registry and env validation tests."
    - "Updated runtime provider registration in apps/orchestrator-api/src/cards.ts."
    - "Updated graph provider enablement in apps/orchestrator-api/src/agent.ts."
  key_links:
    - "Runtime provider registration reads Plane-only defaults from env validation."
    - "Graph construction follows configured providers and does not build Linear graphs by default."
    - "Explicit legacy extra-provider config remains the only non-migration path that registers Linear."
---

<objective>
Cut provider registry, environment validation, and agent graph enablement over to Plane-only active defaults.

Purpose: Make the foundational provider config safe before queue, worker, scheduler, and webhook behavior change per D-01, D-03, and D-05.
Output: Focused tests and runtime code proving Plane-only defaults with explicit legacy extra-provider compatibility.
</objective>

<execution_context>
@/root/.codex/gsd-core/workflows/execute-plan.md
@/root/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-plane-only-provider-cutover/03-CONTEXT.md
@.planning/phases/03-plane-only-provider-cutover/03-RESEARCH.md
@.planning/phases/03-plane-only-provider-cutover/03-VALIDATION.md
@.planning/phases/03-plane-only-provider-cutover/03-PATTERNS.md
@.planning/phases/03-plane-only-provider-cutover/03-01-SUMMARY.md
@CLAUDE.md
@AGENTS.md
</context>

## Source Coverage Audit

| SOURCE | ID | Feature/Requirement | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | - | Remove Linear as an active provider while preserving old-data compatibility. | 03-02 | COVERED | This plan removes implicit provider/env/graph enablement only. |
| REQ | PLN-01 | Linear dependency inventory remains actionable while source/test env changes happen. | 03-02 | COVERED | Static checks keep package/dependency support present. |
| REQ | PLN-02 | Plane is the only active card-provider path unless explicit compatibility is configured. | 03-02 | COVERED | Provider registry/env behavior is the first runtime cutover. |
| REQ | PLN-03 | Plane-focused tests protect active provider behavior. | 03-02 | COVERED | Provider/env/agent tests are RED-GREEN. |
| RESEARCH | R-01 | Stop global/default config from enabling legacy provider. | 03-02 | COVERED | Env and registry tests assert Plane-only defaults. |
| RESEARCH | R-04 | Graph provider binding must follow active provider config. | 03-02 | COVERED | Agent graph enablement is updated here. |
| CONTEXT | D-01 | Plane-only active provider for new work. | 03-02 | COVERED | Task action cites D-01. |
| CONTEXT | D-03 | Test-first Plane characterization and cutover. | 03-02 | COVERED | Task uses RED-GREEN before production edits. |
| CONTEXT | D-05 | Preserve package support and avoid destructive deletion. | 03-02 | COVERED | Task forbids package/dependency deletion. |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED-GREEN provider registry and env validation cutover</name>
  <files>apps/orchestrator-api/src/cards.ts, apps/orchestrator-api/src/cards.test.ts, apps/orchestrator-api/src/env.ts, apps/orchestrator-api/src/env.test.ts</files>
  <read_first>
    .planning/phases/03-plane-only-provider-cutover/03-CONTEXT.md
    .planning/phases/03-plane-only-provider-cutover/03-RESEARCH.md
    .planning/phases/03-plane-only-provider-cutover/03-PATTERNS.md
    .planning/phases/03-plane-only-provider-cutover/03-01-SUMMARY.md
    apps/orchestrator-api/src/cards.ts
    apps/orchestrator-api/src/cards.test.ts
    apps/orchestrator-api/src/env.ts
    apps/orchestrator-api/src/env.test.ts
    vitest.setup.ts
  </read_first>
  <behavior>
    - RED: A Plane-only env import passes with no legacy provider secrets and no legacy extra provider configured.
    - RED: Active runtime rejects legacy primary provider configuration.
    - RED: `createRuntimeCards` registers only Plane with default env and registers Linear only when explicit legacy extra-provider env is supplied.
  </behavior>
  <action>
    RED: Add or adjust failing tests in `cards.test.ts` and `env.test.ts` for the provider/env target behavior. Run the focused test command and record the expected failures before production edits.

    GREEN: Update `env.ts` so active runtime config treats Plane as the only primary provider per D-01. Reject active `CARD_PRIMARY_PROVIDER=linear`; preserve parsing of explicit `CARD_EXTRA_PROVIDERS=linear` for legacy compatibility and require legacy secrets only when that compatibility is enabled. Do not delete legacy env keys because `plane:migrate-linear` and old-row compatibility still need them per D-02 and D-05.

    GREEN: Update `cards.ts` so Plane-only defaults register only Plane. Keep the Linear gateway import and registration path only for explicit legacy extra-provider configuration with valid legacy env. Keep `@agent-platform/linear` and `@linear/sdk` installed per D-05.

    REFACTOR: Remove duplicated provider parsing created during GREEN only if tests stay green; do not introduce a new provider abstraction beyond the existing `CardGatewayRegistry`.
  </action>
  <acceptance_criteria>
    - RED evidence is recorded in the task commit or summary: the new provider/env target tests fail before implementation.
    - Behavior assertion: default runtime registry exposes Plane and rejects unconfigured Linear provider lookup.
    - Behavior assertion: active env validation rejects legacy primary provider config.
    - Behavior assertion: explicit legacy extra-provider config still works when required legacy env is supplied.
    - Source assertion: no package removal occurs for `packages/linear` or `@linear/sdk`.
  </acceptance_criteria>
  <verify>
    <automated>rtk corepack pnpm test -- apps/orchestrator-api/src/cards.test.ts apps/orchestrator-api/src/env.test.ts</automated>
    <automated>rtk bash -lc "rg -n '@agent-platform/linear|@linear/sdk' apps/orchestrator-api/package.json packages/linear/package.json pnpm-lock.yaml"</automated>
  </verify>
  <done>Provider registry and env validation are Plane-only by default and explicit for legacy compatibility.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: RED-GREEN graph provider enablement cutover</name>
  <files>apps/orchestrator-api/src/agent.ts, apps/orchestrator-api/src/agent.test.ts</files>
  <read_first>
    .planning/phases/03-plane-only-provider-cutover/03-CONTEXT.md
    .planning/phases/03-plane-only-provider-cutover/03-RESEARCH.md
    .planning/phases/03-plane-only-provider-cutover/03-PATTERNS.md
    .planning/phases/03-plane-only-provider-cutover/03-01-SUMMARY.md
    apps/orchestrator-api/src/agent.ts
    apps/orchestrator-api/src/agent.test.ts
    apps/orchestrator-api/src/cards.ts
    apps/orchestrator-api/src/env.ts
  </read_first>
  <behavior>
    - RED: Default graph provider set contains Plane and not Linear.
    - RED: Explicit legacy extra-provider config enables Linear graph binding without making Linear primary.
    - RED: `resolveGraphBinding` continues to use the provider-specific `CardGateway` selected from the registry.
  </behavior>
  <action>
    RED: Add failing target tests in `agent.test.ts` for default Plane graph enablement and explicit legacy graph opt-in. Run the focused command and record the expected failures before production edits.

    GREEN: Update `agent.ts` provider graph enablement so the default graph set follows the Plane-only runtime config from Task 1 and does not build a Linear graph unless explicit legacy compatibility is enabled per D-01. Preserve provider-neutral graph node injection through the existing `CardGateway` contract.

    REFACTOR: Keep helper names stable where possible. If provider-list parsing needs a helper, keep it local unless a tested export already exists.
  </action>
  <acceptance_criteria>
    - RED evidence is recorded in the task commit or summary: graph enablement target tests fail before implementation.
    - Behavior assertion: default agent graph creation includes Plane only.
    - Behavior assertion: explicit legacy extra-provider config can still build Linear graph binding.
    - Behavior assertion: report and merge graph dependencies still receive provider-specific gateways from the registry.
  </acceptance_criteria>
  <verify>
    <automated>rtk corepack pnpm test -- apps/orchestrator-api/src/agent.test.ts</automated>
  </verify>
  <done>Agent graph enablement follows Plane-only defaults and keeps Linear only as explicit legacy compatibility.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Env/config -> runtime provider registry | Deployment config controls which card gateways and graphs are active. |
| Runtime registry -> graph construction | Provider enablement determines which external card APIs graph nodes can call. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-03-02-01 | Tampering | `env.ts` / `cards.ts` | high | mitigate | Reject active legacy primary config and only register legacy provider through explicit compatibility env. |
| T-03-02-02 | Elevation of Privilege | `agent.ts` | medium | mitigate | Build graph bindings only for configured providers and keep default graph set Plane-only. |
| T-03-02-03 | Information Disclosure | legacy env keys | medium | mitigate | Keep legacy secrets optional unless explicit compatibility is enabled; no real secrets in tests or examples. |
| T-03-02-SC | Tampering | package installs | high | mitigate | No package installs are planned; package/dependency deletion is explicitly forbidden by D-05. |
</threat_model>

<verification>
Run focused checks after tasks:

<automated>rtk corepack pnpm test -- apps/orchestrator-api/src/cards.test.ts apps/orchestrator-api/src/env.test.ts apps/orchestrator-api/src/agent.test.ts</automated>
<automated>rtk bash -lc "rg -n '@agent-platform/linear|@linear/sdk' apps/orchestrator-api/package.json packages/linear/package.json pnpm-lock.yaml"</automated>
</verification>

<success_criteria>
- Plane-only defaults pass provider/env/agent tests without legacy secrets or legacy extra provider config.
- Explicit legacy compatibility remains available only when configured.
- `packages/linear` and `@linear/sdk` remain present.
- No queue, worker, scheduler, webhook, schema, dashboard, or docs files are modified by this plan.
</success_criteria>

<output>
Create `.planning/phases/03-plane-only-provider-cutover/03-02-SUMMARY.md` when done.
</output>
