---
phase: 05-orchestrator-hub-refactor
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/orchestrator-api/src/routes/routeAuth.ts
  - apps/orchestrator-api/src/routes/routeAuth.test.ts
  - apps/orchestrator-api/src/routes/rendering.ts
  - apps/orchestrator-api/src/routes/rendering.test.ts
  - apps/orchestrator-api/src/routes/agents.ts
  - apps/orchestrator-api/src/routes/tools.ts
  - apps/orchestrator-api/src/routes/schedules.ts
  - apps/orchestrator-api/src/routes/admin.ts
  - apps/orchestrator-api/src/routes/registry.ts
autonomous: true
requirements:
  - REF-01
  - VER-01
user_setup: []
must_haves:
  truths:
    - "REF-01: duplicated route-local bearer auth is replaced by one shared helper without changing protected/open route coverage."
    - "REF-01: duplicated route-local HTML/date helpers are replaced by shared helpers without weakening escaping or changing test-visible output."
    - "VER-01: helper behavior is characterized before route imports move."
    - "D-01/D-06: public route statuses, JSON bodies, and auth behavior remain stable."
  artifacts:
    - path: "apps/orchestrator-api/src/routes/routeAuth.ts"
      provides: "Shared RUNNER_AUTH_TOKEN bearer middleware"
    - path: "apps/orchestrator-api/src/routes/routeAuth.test.ts"
      provides: "Fail-first auth helper characterization"
    - path: "apps/orchestrator-api/src/routes/rendering.ts"
      provides: "Shared escapeHtml, formatDate, and humanizeStatus primitives"
    - path: "apps/orchestrator-api/src/routes/rendering.test.ts"
      provides: "Fail-first rendering helper characterization"
  key_links:
    - from: "apps/orchestrator-api/src/routes/agents.ts"
      to: "apps/orchestrator-api/src/routes/routeAuth.ts"
      via: "protected POST/PATCH middleware"
      pattern: "requireRunnerAuth"
    - from: "apps/orchestrator-api/src/routes/schedules.ts"
      to: "apps/orchestrator-api/src/routes/routeAuth.ts"
      via: "protected schedule route middleware"
      pattern: "requireRunnerAuth"
    - from: "apps/orchestrator-api/src/routes/admin.ts"
      to: "apps/orchestrator-api/src/routes/rendering.ts"
      via: "Mission Control escaping/date/status text helpers"
      pattern: "escapeHtml"
    - from: "apps/orchestrator-api/src/routes/registry.ts"
      to: "apps/orchestrator-api/src/routes/rendering.ts"
      via: "registry escaping/date helpers"
      pattern: "formatDate"
---

<objective>
Extract shared route/auth/render helpers with tests while preserving every existing route contract.

Purpose: Phase 5 needs stable shared primitives before the webhook and Mission Control hubs are split.
Output: Orchestrator-local helper modules and focused tests consumed by existing route modules.
</objective>

<execution_context>
@/root/.codex/gsd-core/workflows/execute-plan.md
@/root/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-orchestrator-hub-refactor/05-CONTEXT.md
@.planning/phases/05-orchestrator-hub-refactor/05-RESEARCH.md
@.planning/phases/05-orchestrator-hub-refactor/05-PATTERNS.md
@.planning/phases/05-orchestrator-hub-refactor/05-VALIDATION.md
@.planning/phases/04-operational-flow-reorganization/04-01-SUMMARY.md
@.planning/phases/04-operational-flow-reorganization/04-02-SUMMARY.md

@apps/orchestrator-api/src/routes/admin.ts
@apps/orchestrator-api/src/routes/admin.test.ts
@apps/orchestrator-api/src/routes/registry.ts
@apps/orchestrator-api/src/routes/registry.test.ts
@apps/orchestrator-api/src/routes/agents.ts
@apps/orchestrator-api/src/routes/agents.test.ts
@apps/orchestrator-api/src/routes/schedules.ts
@apps/orchestrator-api/src/routes/schedules.test.ts
@apps/orchestrator-api/src/routes/tools.ts
@apps/orchestrator-api/src/routes/tools.test.ts
</context>

<dirty_worktree>
Preserve unrelated untracked `.planning/PROJECT.md`, `.planning/config.json`, `.planning/phases/01-bootstrap-and-architectural-inventory/`, `.planning/phases/02-living-documentation-and-historical-archive/`, `.planning/research/`, and `docs/superpowers/README.md`. Do not add, format, delete, or revert those paths.
</dirty_worktree>

<source_coverage_audit>
| SOURCE | ID | Feature/Requirement | Plan | Status | Notes |
|--------|----|---------------------|------|--------|-------|
| GOAL | - | Split high-risk orchestrator hubs and duplicated route utilities into smaller modules without changing behavior. | 05-01, 05-02, 05-03 | COVERED | Helpers first, then webhook and admin/Mission Control seams. |
| REQ | REF-01 | Shared route/auth/render helpers replace duplicated `requireAuth`, `escapeHtml`, `formatDate`, and similar local copies where appropriate. | 05-01, 05-03 | COVERED | 05-01 extracts shared primitives; 05-03 uses them in Mission Control rendering. |
| REQ | REF-02 | Webhook handling is split into provider-neutral intake and Plane-specific parsing/transition logic. | 05-02 | COVERED | 05-02 extracts signature, Plane parser, and run transition seams. |
| REQ | VER-01 | Characterization tests protect behavior before each risky refactor. | 05-01, 05-02, 05-03 | COVERED | Every implementation task is fail-first/TDD before moving behavior. |
| RESEARCH | R-01 | No package installs, dependency upgrades, or renderer framework adoption. | 05-01, 05-02, 05-03 | COVERED | Plans include package-file checks and supply-chain stop gates. |
| RESEARCH | R-02 | Use orchestrator-local helper modules, not new packages. | 05-01, 05-02, 05-03 | COVERED | New files stay under `apps/orchestrator-api/src`. |
| RESEARCH | R-03 | Hono route tests use direct `app.request` and Vitest mocks, avoiding live Plane/GitHub/Redis dependencies. | 05-01, 05-02, 05-03 | COVERED | Verification commands are local Vitest/typecheck/full verify only. |
| CONTEXT | D-01 | Behavior-preserving refactor; preserve routes, statuses, JSON, HTML-visible strings/classes, queue payloads, transitions, and auth behavior. | 05-01, 05-02, 05-03 | COVERED | Each plan names stable behavior and route tests. |
| CONTEXT | D-02 | Add/tighten characterization tests before moving behavior. | 05-01, 05-02, 05-03 | COVERED | TDD/fail-first tasks required. |
| CONTEXT | D-03 | Prefer small orchestrator-local modules; no dependencies or frontend/rendering framework. | 05-01, 05-02, 05-03 | COVERED | All new modules are local; install stop gate present. |
| CONTEXT | D-04 | Use narrow commits by seam. | 05-01, 05-02, 05-03 | COVERED | Plan boundaries are shared helpers, webhook seams, admin/Mission Control seams. |
| CONTEXT | D-05 | Shared helper extraction covers bearer auth, escaping, date formatting, status/pill classes, and small HTML helpers. | 05-01 | COVERED | Route auth and rendering helpers are the plan objective. |
| CONTEXT | D-06 | Preserve `RUNNER_AUTH_TOKEN` bearer-token contract and route coverage. | 05-01 | COVERED | Auth tests cover exact comparison and protected/open routes. |
| CONTEXT | D-07 | HTML helpers stay boring and explicit and must not sanitize less than current helpers. | 05-01, 05-03 | COVERED | Rendering tests include malicious values. |
| CONTEXT | D-08 | Keep `/webhooks/plane` active and `/webhooks/linear` legacy compatibility only. | 05-02 | COVERED | 05-02 static and route tests preserve both paths. |
| CONTEXT | D-09 | Split webhook signature, Plane payload, label transition, removal/cancel, enqueue, approval resume, and skip logging concerns. | 05-02 | COVERED | 05-02 task split follows these responsibilities. |
| CONTEXT | D-10 | Preserve idempotency and safety behavior for duplicates, pause/cost, removals, and approval resume. | 05-02 | COVERED | 05-02 run action tests cover these branches. |
| CONTEXT | D-11 | Mission Control remains read-only; no launch/replay/approve/retry/cancel/deploy controls. | 05-03 | COVERED | 05-03 render tests preserve read-only copy. |
| CONTEXT | D-12 | Split admin/Mission Control route handlers, summary/detail data, JSON shape, dashboard/detail rendering, and formatting helpers. | 05-03 | COVERED | 05-03 extracts data/render modules. |
| CONTEXT | D-13 | Preserve current HTML inspection behavior and test-visible copy. | 05-03 | COVERED | 05-03 route/render tests protect copy and content type. |
| CONTEXT | D-14 | Do not refactor worker execution, `runJob.ts`, `codegen.ts`, eval harness, or data-collector internals in Phase 5. | 05-01, 05-02, 05-03 | COVERED | Files_modified exclude those modules. |
| CONTEXT | D-15 | Do not change database schema, provider env defaults, Tailscale Funnel, Plane labels, or production deployment configuration. | 05-01, 05-02, 05-03 | COVERED | Plans are source/test only. |
| CONTEXT | D-16 | If a seam exceeds scope, record the gap and do not expand Phase 5 into a broad rewrite. | 05-01, 05-02, 05-03 | COVERED | Tasks instruct executors to stop on scope expansion. |
</source_coverage_audit>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extract shared runner auth middleware</name>
  <files>apps/orchestrator-api/src/routes/routeAuth.ts, apps/orchestrator-api/src/routes/routeAuth.test.ts, apps/orchestrator-api/src/routes/agents.ts, apps/orchestrator-api/src/routes/tools.ts, apps/orchestrator-api/src/routes/schedules.ts</files>
  <behavior>
    - RED: `requireRunnerAuth` rejects missing and wrong `authorization` headers with status 401 and `{ error: "unauthorized" }`.
    - RED: `requireRunnerAuth` calls `next()` only when the header is exactly `Bearer ${RUNNER_AUTH_TOKEN}`.
    - RED: `GET /agents` and `GET /tools` remain open, while `POST` and `PATCH` stay protected.
    - RED: `/schedules` and `/schedules/*` remain protected.
  </behavior>
  <action>Create `routes/routeAuth.ts` with a single exported Hono middleware for the shared runner bearer contract per D-05 and D-06. Write `routeAuth.test.ts` first and run it before implementation to prove the missing export or behavior fails for the right reason per D-02 and VER-01. Then replace local `requireAuth` copies in `agents.ts`, `tools.ts`, and `schedules.ts` with the shared helper while preserving existing route coverage exactly: agents/tools reads stay open, agents/tools writes stay protected, and all schedules routes stay protected. Keep this as an orchestrator-local module per D-03. Stop if a package install appears necessary; none is planned.</action>
  <verify>
    <automated>rtk corepack pnpm vitest run apps/orchestrator-api/src/routes/routeAuth.test.ts
rtk corepack pnpm vitest run apps/orchestrator-api/src/routes/agents.test.ts apps/orchestrator-api/src/routes/tools.test.ts apps/orchestrator-api/src/routes/schedules.test.ts
rtk corepack pnpm --filter @agent-platform/orchestrator-api typecheck</automated>
  </verify>
  <done>`routes/routeAuth.ts` is the only auth helper used by agents/tools/schedules, all existing protected/open route tests pass, and package files remain unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extract shared rendering primitives</name>
  <files>apps/orchestrator-api/src/routes/rendering.ts, apps/orchestrator-api/src/routes/rendering.test.ts, apps/orchestrator-api/src/routes/admin.ts, apps/orchestrator-api/src/routes/registry.ts</files>
  <behavior>
    - RED: `escapeHtml` escapes `&`, `<`, `>`, `"`, and `'` and treats nullish input as an empty string.
    - RED: `formatDate` returns `-` for nullish values and preserves the current ISO date display format.
    - RED: `humanizeStatus` preserves underscore-to-title display used by Mission Control.
    - RED: admin auth remains protected with the shared runner helper and registry/admin HTML output remains escaped.
  </behavior>
  <action>Create `routes/rendering.ts` for `escapeHtml`, `formatDate`, and `humanizeStatus` per D-05 and D-07. Write `rendering.test.ts` first and run it before implementation to lock the current escaping/date/status text behavior per D-02 and VER-01. Then update `admin.ts` and `registry.ts` to import the shared primitives, and update `admin.ts` to use `requireRunnerAuth` for `/admin/*` without changing the route's 401 body or status per D-01 and D-06. Keep route-specific status-class mappings local where their meanings differ. Do not introduce a rendering framework, JSX renderer, package dependency, schema change, provider env default, or UI control per D-03, D-11, D-14, and D-15.</action>
  <verify>
    <automated>rtk corepack pnpm vitest run apps/orchestrator-api/src/routes/rendering.test.ts
rtk corepack pnpm vitest run apps/orchestrator-api/src/routes/admin.test.ts apps/orchestrator-api/src/routes/registry.test.ts apps/orchestrator-api/src/routes/agents.test.ts apps/orchestrator-api/src/routes/tools.test.ts apps/orchestrator-api/src/routes/schedules.test.ts
rtk corepack pnpm --filter @agent-platform/orchestrator-api typecheck
rtk git diff --exit-code -- package.json pnpm-lock.yaml apps/orchestrator-api/package.json</automated>
  </verify>
  <done>Admin and registry use shared rendering primitives, admin uses shared runner auth, all helper/route characterization tests pass, and no package files changed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator/client -> protected Hono route | Untrusted requests cross into admin, schedule, agent-write, and tool-write handlers. |
| Database values -> server-rendered HTML | Stored agent/tool/run/scenario values are interpolated into registry and Mission Control HTML. |
| Planner/executor -> package manager | Refactor work may be tempted to install helper packages even though existing stack is sufficient. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-01-01 | Elevation of Privilege | `routes/routeAuth.ts`, `routes/admin.ts`, `routes/agents.ts`, `routes/tools.ts`, `routes/schedules.ts` | high | mitigate | Preserve exact `RUNNER_AUTH_TOKEN` bearer comparison and 401 JSON through helper and route tests. |
| T-05-01-02 | Tampering | `routes/rendering.ts`, `routes/admin.ts`, `routes/registry.ts` | high | mitigate | Preserve escaping for all current characters and add malicious-value tests before route imports move. |
| T-05-01-03 | Information Disclosure | `routes/admin.ts` | medium | mitigate | Keep `/admin/*` protection unchanged while swapping to the shared helper. |
| T-05-01-04 | Repudiation | route helper extraction | low | accept | This plan does not change runtime logging; existing route tests and summaries provide refactor evidence. |
| T-05-01-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package installs are planned; if an executor believes one is required, stop before changing package files and create a blocking package-legitimacy checkpoint. |
</threat_model>

<verification>
Run task-level commands after each task. Before completing this plan, run:

<automated>rtk corepack pnpm vitest run apps/orchestrator-api/src/routes/routeAuth.test.ts apps/orchestrator-api/src/routes/rendering.test.ts apps/orchestrator-api/src/routes/admin.test.ts apps/orchestrator-api/src/routes/registry.test.ts apps/orchestrator-api/src/routes/agents.test.ts apps/orchestrator-api/src/routes/schedules.test.ts apps/orchestrator-api/src/routes/tools.test.ts
rtk corepack pnpm --filter @agent-platform/orchestrator-api typecheck
rtk git diff --exit-code -- package.json pnpm-lock.yaml apps/orchestrator-api/package.json</automated>
</verification>

<success_criteria>
- REF-01 shared route/auth/render helper extraction is complete for the safe duplicated route utilities.
- VER-01 is satisfied for this seam by fail-first helper tests plus route characterization tests.
- Public route auth behavior, open reads, protected writes, HTML escaping, date formatting, and status text remain stable.
- No package installs, schema/deploy/provider env changes, worker/eval internals, Mission Control controls, or Linear active-provider changes occur.
</success_criteria>

<output>
Create `.planning/phases/05-orchestrator-hub-refactor/05-01-SUMMARY.md` when done.
</output>
