# Agent Instructions

This project keeps agent-facing instructions in one canonical file:

- [`CLAUDE.md`](./CLAUDE.md)

All agents should follow `CLAUDE.md` for project rules, commit conventions,
Linear sync expectations, and RTK command usage. Keep this file as a pointer to
avoid duplicated instructions drifting over time.
